# Recent death review

**Evades enemy profile:** `ravel-transfer-v1`; the WW19 correction was revision `699761930e1e`. Subsequent shared CLI changes add a separate [Ravel browser mode](ravel-assist.md), with current hashes recorded in the handoff. The user authorized the [Ravel-to-live port](live-port.md) after the [pre-Wacky rollback](pre-wacky-rollback.md). The broad Wacky learner remains removed; targeted Switch, Spiral and Zoning corrections use recorded evidence. Historical revisions and their validation appear below.

## September 21: late-evening update lag and WW7

The user reported noticeably worse game lag late in the day. `death-1790058622764.json` (23:30 PDT, WW7) still ran older revision `cead0cca1cc2`; Wavy enemy 546/type241 overlaps the final player by 2.44 units. The last 3.92 seconds contain four gaps over 100 ms between fresh game-state observations, with a maximum of 124.3 ms. During that window browser observation calls took 4.16 ms median, input calls 0.82 ms, and actual planning calls 13.95 ms. The native packet counter advanced about 60.26 ticks/second overall, with pauses followed by catch-up.

Earlier WW samples at 18:48, 20:22 and 22:53 PDT had maximum fresh-update gaps of 37.5, 79.9 and 55.4 ms. The recent 23:11 and 23:16 recordings reach 549.6 and 432.0 ms. These short, different encounters support worsening update-delivery jitter while local browser calls remain responsive; they do not isolate server processing from network delays or client buffering. Native ping RTT and server processing times were not recorded. Wavy prediction remains a separate limitation, so this review does not attribute the whole death to lag.

Two attached controller processes were present during inspection: older `cead0cca1cc2` and updated `699761930e1e`. The updated process's log remained paused throughout the overlapping run; the old process produced the death. This explains why the latest packet-gap correction was not controlling that run, but does not prove two active controllers caused the update stalls. No process was stopped and no game setting was changed.

The current public client confirms Settings → Interface → Display FPS/Ping, available server selection, and Visual options for Ability Particles, Cosmetic Effects, Background Objects and Confetti. Display FPS/Ping also displays the reported tick rate. Compare latency across available servers; reduced effects can help rendering but do not shorten server/network waits. Its locked renderer skips identical packets, so falling displayed FPS alone does not prove a graphics bottleneck. Experimental client prediction smooths presentation and has documented enemy-position errors; it is not a source of fresh authoritative updates. Chrome's Energy Saver can affect gaming performance. Details, timings and source provenance are in `artifacts/lag-settings-review.json`; this investigation changed no controller behavior.

## September 21: Wacky Wonderland 19 — Zoning deceleration and a missed turn during a packet gap

`death-1790057484504.json` ran the live-port revision `cead0cca1cc2`. Zoning enemy 457 (type 249) overlaps the final player by 3.61 units. The player was moving at base speed 510 plus Magmax Flow's 180 bonus, or 690 world units/second. The downed snapshot loses that bonus; earlier observations contained it correctly.

The collider slows by 450 units/second²: speed 150 at packet 5806, 37.5 at 5821 and 15 at 5824. A constant-velocity forecast places it 15 and 21.375 units beyond the two later recorded positions. `MotionTracker` now accepts a deceleration only after two consecutive intervals agree in both velocity change and displacement. Forecasts update speed before movement at native ticks and share that path with navigation. Inconsistent motion, effects, phase changes, stops and gaps over six ticks invalidate the fit. The model uses past observations only; both held-out position errors are below 0.01 units.

Packet 5806 was repeatedly read for roughly a quarter second. Fast browser reads made the snapshot appear only a few milliseconds old, while the duplicate-packet branch kept holding `up_right`. The retained plan called for `up` at relative tick 13; that follow-up was never issued during the gap. By the next new packet, every forecast already collided in the committed-input prefix.

`PacketClock` now preserves the first capture time of each game packet. The CLI rechecks an unchanged packet after 25 ms with an 8 ms optional-search budget, allowing retained turns and safety overrides to be issued as the state ages. Queued-input reconstruction includes commands sent since the original capture, retaining their arrival times. Fresh packets retain the 18 ms budget; 750 ms without an update still pauses movement. Traces distinguish browser-read age from `packetAgeMs`, and `stateAt` records the original state timestamp. These search budgets do not bound navigation, mandatory checks, browser calls or OS pauses.

The independent short replay starts at packet 5806 and uses later recorded enemy positions, interpolating missing ticks. It covers 0.3 seconds with 125 ms command delay plus 13 ms fixed computation/delivery, perturbing pending and new input arrivals by −1/0/+1 tick. Holding the old input reproduces the collision at the latest arrival (enemy 457, −3.95 units clearance). The corrected controller survives all three with 207 units of forward progress each and minimum clearances of 40.62, 33.03 and 25.03 units. Replanning without the Zoning correction also survives this window, but progresses only 141.83–164.82 units. Exhausting optional search entirely still follows a surviving retained turn.

All **171 tests pass**. Repeating the replay ten times per input timing with the actual 8 ms search budget gives 30/30 corrected survivals and 207 units of progress throughout. Local planning alone measures 6.35 ms median, 7.41 ms p95 and 8.06 ms maximum in these samples; measured cost is reported separately and does not replace the replay's fixed delivery allowance. Six earlier OOH 10 / Cata 35 recorded windows retain identical survival, progress, clearance and turn metrics against the pre-fix snapshot. That separate harness compares fresh-snapshot behavior and does not exercise duplicate-packet timing.

Evidence: `test/fixtures/ww19-zoning.json`, `test/ww19.test.js`, and `artifacts/ww19-fix/` (pre-fix source, gap replay scripts/reports, paired earlier encounters, and test output). This establishes the reproduced encounter, not a complete live WW clear. Zoning's later stop duration and perpendicular-turn direction remain unknown; the forecast holds its stop position until new observations establish motion. Wavy phase timing, including the older WW48 trace, remains unresolved. No live browser was controlled during validation.

## September 21: Wacky Wonderland 14 — a Spiral projected straight

`death-1790047330558.json` ran revision `e6eda42e4f39`. The collider was Spiral enemy 1265 (type 206), with 1.57 units of overlap in the final snapshot. The last 40 decisions had median planning of 13.56 ms (maximum 22.98), median input overhead of 0.99 ms (maximum 2.38), and about 3.26 ms snapshot age. It was a curved-motion prediction failure: the ordinary velocity projection pointed away from the player's approach while the ball kept circling back.

Revision `d4fd8dc24640` adds a bounded curvature fit only for Spiral and Spiral Switch types 206/207. It keeps at most nine native velocity observations per body, fits changing angular velocity on older intervals, and requires two newer intervals to agree before using the model. Duplicate packets do not retrain it. Area changes, packet resets, gaps exceeding three ticks, speed/effect changes, disappearance and inconsistent turns invalidate the fit. The forecast integrates at server ticks and shares the same interpolated path between local collision checks and coarse navigation, with three units of fitted-path padding. Other enemy families do not run this fit; the broad period learner and uncertainty regions remain disabled.

The regression uses actual recorded history and later enemy positions, with no reconstructed hidden bodies or future observations in the fit. At frame 185, the curved prediction stays within 0.0032 units of the later recorded collider positions; the straight projection deviates by up to 99.91 units. The recorded `right` candidate changes from 57.15 units of forecast clearance to overlap, and the updated search finds `up_right` with 38.77 units of forecast clearance before the fatal movement is committed.

All 131 tests pass. Nine short closed-loop windows start at frames 175, 180 and 185 and vary actual input arrival by minus/plus one tick. The baseline collides in all nine; the updated planner survives all nine, preserves the same forward progress, and uses no Shift in those windows. The lowest updated physical clearance is 36.61 units. These windows last only 0.3–0.5 seconds and use recorded player-independent enemy positions with interpolation between packets. They do not establish a full level or live clear. The replay harness now passes the recorded input-delay estimate to movement settling, matching the live controller.

All 340 compared non-Spiral Cata candidate results are identical. Paired profiling with the same 18 ms optional-search budget gives Cata median tracking/navigation/planning of 18.12 ms for both versions; Spiral median rises from 11.69 to 12.62 ms, and p95 from 24.32 to 26.18 ms. Spiral tracking itself takes about 0.038 ms median. Reports are `artifacts/spiral14-death-review.json`, `artifacts/replay-spiral14.json`, and `artifacts/profile-spiral14.json`. The regression fixture is `test/fixtures/spiral14.json`.

The fit extrapolates recent curvature, not complete server phase rules. Warmup, unobserved phase changes and wall behavior beyond the validated window remain limitations; rejected fits fall back to the existing motion forecast rather than retaining stale curvature.

## September 21: Wacky Wonderland 36 — Switch body missing before activation

`death-1790041720559.json` ran revision `4912363e4079` in displayed Wacky Wonderland Area 36. Switch enemy 1658 (type 217) overlaps the final player by 0.83 units. Across the last 50 decisions, median planning was 15.19 ms (maximum 24.00), median input overhead 1.11 ms (maximum 2.46), and median snapshot age 4.03 ms. The up-right turn was visible in native submitted keys on the next recorded input sequence, about 33 ms after application, and upward movement appeared about 124 ms after application. No comparable computation or input-delivery stall is present.

The observer's harmless filter omitted the entire next group of Switch bodies. Enemy 1658 first appears at frame 195, packet 16968, only seven ticks before the downed snapshot. At frame 193 the already-issued up-right turn was forecast with 46.21 units of physical clearance; after the new bodies appeared, every action had a collision in its queued-input prefix. The collider's ordinary movement forecast from the final five observations matches its final position within 0.004 units. Earlier visibility of its activation is the missing information.

Revision `e6eda42e4f39` restores only the targeted Switch activation handling: retain harmless Switch bodies and their native countdown, check collision geometry from activation with a one-tick allowance, and include that timing in coarse routes. Missing countdowns grant no safe crossing. Already-lethal enemies are never assumed to become safe on a future phase. Bodies that remain harmless past the forecast horizon skip trajectory work. The broader Wacky motion learner and other removed enemy models remain disabled; the controller keeps the pre-Wacky profile with this explicit exception.

All 125 tests pass, including observation, missing timers, safe crossings, mid-segment activation, homing collision branches, route costs and a recorded-state reconstruction. At frame 185, adding backward-reflected positions for the 18 newly visible Switch bodies lowers the recorded down-right route's physical clearance from 62.72 to 6.33 units; the corrected search finds a buffered alternative even with activation shifted by minus/plus one tick. These pre-activation positions and countdowns were not logged: the fixture explicitly labels their reconstruction from the first lethal observation and the activation packet bracket. It is not a complete counterfactual survival result or a verified live clear.

The 340 paired non-Switch local candidate results are identical. Paired 18 ms-budget profiling keeps Cata 23 median navigation-plus-planning at 18.10 versus 18.09 ms. Wacky median is 18.03 versus 18.07 ms; p95 rises from 37.59 to 50.39 ms while incorporating the additional Switch bodies, so peak route cost remains a limitation. Reports are `artifacts/switch36-death-review.json`, `artifacts/switch36-frame-review.json`, and `artifacts/profile-switch36.json`; regression data is `test/fixtures/switch36-activation.json`.

## September 21: fewer optional turns and faster input delivery

Revision `4912363e4079` follows the Area 23 input-arrival review below. Movement and Candy key changes now share one synchronous browser call with the final pause/epoch check. Unchanged keys stay held; each reversal releases and presses its keys before the client's input sampler can run again. Pausing, chat focus, navigation and manual control are checked separately. Ordinary keyboard events still go through the game's handlers; no game or network state is modified directly.

When both routes remain safe, a discretionary direction change waits until the last command has had its estimated delivery time. Emergency overrides, scheduled turns, stalls and clear full-speed shortcuts remain eligible immediately. This uses the existing movement-derived delay estimate, not server acknowledgement. The live optional-search budget falls from 24 to 18 ms; mandatory complete-path checks and the existing timing correction remain. Logs now include key-change counts and the native last-submitted keys/sequence to help distinguish controller changes from client submission in the next run.

Validation: all 120 tests pass. An isolated headless Chrome page confirms complete key transitions, no repeated held-key events, stale-pause rejection, manual arrows while paused, chat focus and navigation handling. Across 60 measured multi-key transitions per method, median local input overhead falls from 1.94 to 0.39 ms; p95 falls from 4.75 to 0.85 ms (`artifacts/input-batch-review.json`). This measures local browser delivery, not network latency.

Both revisions clear the same 12 simplified dasher courses at 660 speed and 135 ms configured input delay. Action changes fall from 186 to 176 (5.4%); reversals fall from 57 to 45 (21.1%). Aggregate simulated clear time changes from 52.30 to 51.87 seconds, while focused movement totals 6.34 versus 6.43 seconds. CPU/browser time is not added to simulated travel time. The comparison is summarized in `artifacts/input-efficiency-review.json`, with final course rows in `artifacts/benchmark-input-efficiency-final.json`.

Paired profiling of Area 23 frames 140–199, with one warmup and three measured passes, reduces median navigation-plus-planning time from 24.08 to 18.07 ms and p95 from 34.44 to 27.15 ms. Both produce buffered forecasts in 144 of 180 calls with the same recorded inputs and prior plans (`artifacts/profile-input-efficiency-cata23.json`). Those are fixed-snapshot forecasts, not proof of surviving the whole encounter. Server arrival jitter and acknowledgement remain unresolved, and a live clear with this revision is unverified.

## September 21: Catastrophic Core Area 23 input-arrival mismatch

The next death, `death-1790038871206.json`, ran the updated `72dff445d360` revision in displayed Area 23. Dasher 118 overlaps the final player position near the top wall by 8.27 units. Planning median is 24.07 ms; the final decisions take 24–36 ms including navigation, with no recurrence of the earlier 166 ms search stall. From the final five sampled forecast starts, the dasher's predicted final position differs from the recording by only 0.001–0.025 units.

The recorded input schedule is inconsistent with the final player motion. At frame 194, the bot requests `up_left`; 168.42 ms later, frame 199 still moves at approximately `(345, -345)`, matching the earlier `focus_up_right`. The delay estimate remains 125 ms. Replaying frame 196's queued inputs to frame 199's packet predicts `(344329.22, 15.00)` and leftward movement; the actual position is `(344379.53, 21.41)` and still rightward, a 50.72-unit position error. The planner consequently credits an unobserved retreat with creating room before the incoming dasher. The later `down_right` request has not yet appeared in movement by the last alive sample either.

This diagnosis identifies an input-arrival mismatch, not a new enemy-motion or computation-time failure. Native sent-input sequence and server acknowledgement were not recorded, so these logs cannot distinguish a delayed delivery from a missed intermediate turn. This review does not change runtime behavior or establish a full counterfactual survival result. Measurements and the queued-input comparison are saved in `artifacts/cata23-input-review.json`. Input confirmation and handling sudden arrival jitter remain unresolved.

## September 21: Catastrophic Core Area 34 planning delay

`death-1790037452247.json` ran revision `482f247cc953`. The final snapshot overlaps homing enemy 185 by 5.60 units. Area 34 contains 38 homing enemies, 24 slowing enemies and eight perimeter enemies. The live player has base speed 510 plus Magmax Flow's 180 bonus (690 outside slowing fields, 537 inside); the bonus disappears in the downed snapshot. Forecasting the collider along the recorded player path misses its final position by about 1.1–1.4 units, so there is no evidence here for changing the homing turn model.

Frame 198 spends 165.55 ms planning, compared with 41.27 ms on the previous frame. The new-command forecast still budgets the previous calculation's cost: 185.16 ms total reaction time, versus roughly 308 ms after including the recorded snapshot-to-application interval and fitted 125 ms input delay. That leaves approximately seven extra server ticks of old movement before the requested dodge. The next frame already has no collision-free escape under the recorded queued inputs. The exact pre-spike observation is preserved in `test/fixtures/cata34-latency.json`.

Live planning now reserves 24 ms after navigation's actual completion. It checks constant-input fallback trajectories and the previous plan before branching, prioritizes promising first turns, finds inexpensive multi-turn escapes across inputs, and spends remaining time on wider alternatives. Interrupted searches are discarded; only fully checked paths or known collisions are returned. Focused recovery precedes wider search when the cheaper pass finds no buffered escape. Existing turn deadlines, safety margins, and boosted movement physics remain intact. Dodge commitment is evaluated at decision time, so time spent planning counts toward completing the commitment.

Command timing uses the current calculation's reservation rather than the previous frame's duration. If actual computation differs by more than a server tick, one shorter recheck uses the newly measured time. This handles both a long pause and a quick shortcut that would otherwise send a precise rescue turn too early. The budget limits optional work; mandatory forecasts and checks, garbage collection, browser latency and OS scheduling can still exceed it. Logs expose `searchLimited`, `searchedRoots`, `lagRecheck` and `timingRecheck`.

Paired local checks on frames 180–199, with identical recorded queues, reaction budgets, prior plans and cached navigation, reduced median total planning from 47.24 to 24.15 ms and p95 from 69.53 to 36.27 ms. Both planners find at least one buffered route in 48/60 measured calls (three passes over 20 snapshots). Local-search p95 falls from 56.42 to 24.23 ms. The deterministic deadline regression finds a full escape with 42.06 units physical clearance after the first cheap branch, and rejects incomplete branches when interrupted earlier. See `artifacts/cata34-planning-review.json`.

The final revision is `72dff445d360`. All 114 automated checks pass. Both the baseline and final bounded planner clear 12/12 simplified dasher courses at 660 speed and 135 ms configured input delay, with the same per-course clear times and command-change counts. On those simpler courses, final median planning is 17.34 versus 16.76 ms before; p95 is 24.06 versus 23.85 ms. The preliminary pass is restricted to homing or crowded forecasts to avoid unnecessary extra work in sparse rooms. See `artifacts/benchmark-cata34-budget-dash-final.json` and the baseline rows in `artifacts/benchmark-cata34-budget-dash.json`. These courses model fixed command delay and do not include measured computation in simulated travel time.

These are recorded-snapshot forecasts and local timings, not a counterfactual full-run survival result. Replaying recorded enemy positions after changing the player's path would be invalid for this homing encounter. A live Area 34 clear remains unverified.

## September 21: Catastrophic Core Area 37 input delay

The newest traces, `death-1790031387678.json` (Shade) and `death-1790030989209.json` (Magmax), both ran revision `7bc9234b634f`. Their live frames correctly included the 150/180 speed bonuses. Those bonuses disappear in the final downed snapshot; that snapshot alone would misleadingly suggest a missing speed modifier.

Both traces hold a 125 ms input-delay estimate throughout the final maneuvers. The fitter previously skipped every interval near a slowing aura, preventing updates in much of this area. It now reconstructs the aura positions between the two recorded packets for each possible queued-input path, applies each slow type once, and respects shelter and aura immunity. It still skips unknown/disappearing nearby fields and changes in player speed modifiers. Timing-sample counts are now logged.

Replaying the observed inputs yields 72 usable fits instead of 16 in the Shade trace and 166 instead of 33 in the Magmax trace, ending at a 150 ms estimate. In Shade frame 198, the old estimate reports +20.365 physical clearance for the recorded `focus_down` command; recalibration predicts −0.080 and chooses `right`, with +21.644 predicted clearance. This verifies a false-safe prediction is rejected. It does not prove survival through the complete encounter, and the alternate route still falls short of the normal safety buffer. The regression fixture is `test/fixtures/cata-delay.json`.

The same update adds a one-person rescue objective, available through the visible **Rescue nearest (R)** button or **R** while running. Selection is restricted to downed, revivable players in the current area's entity roster and bounds. Navigation blocks area exits, routes to body contact, and keeps checking hazards beyond contact; it does not treat reaching a downed player as invulnerability. Revival, disappearance, area changes, pause, or cancellation discard the rescue route and pending model decision.

Validation: 104 tests passed, including the two recorded Cata sequences, a synthetic latency increase inside overlapping slowing fields, rescue selection and cancellation, routing around a wall, and hazard checks after contact. An isolated headless Chrome session checked button visibility, clicking, cancellation, stale decision rejection, chat input, Escape and removal on disposal. All 12 generated dasher courses cleared at 660 speed with 135 ms input delay (`artifacts/benchmark-rescue-cata-dash.json`). These are offline checks; live rescue and Cata completion remain unverified.

## Rescue approach follow-up

The first live rescue run (`logs/2026-09-21T23-53-39.439Z.jsonl`, revision `d64cb0cdc1ed`) confirms R selected player 170 in Central Core Hard Area 40. The first attempt was cancelled; a later attempt ended when the target stopped qualifying. The old log cannot distinguish revival from disappearance, so that status is not proof of a successful save. The user subsequently confirmed rescue works but reported janky movement.

An independent delayed-input approach test reproduced an empty-area overshoot: from (200,250) to (780,380), the earlier controller continued down-right to the boundary and reversed up-left before contact at tick 74. The rescue planner now checks a direct point approach with separately timed axis releases and a body-sized alignment tolerance. It retains those releases at their original packet deadlines instead of recomputing opposite corrections before queued keys arrive. Braking and the entire post-contact wait are checked for hazards; an unsafe or obstructed approach falls back to ordinary route search. Switching objectives clears stale turns and commitments without sending an artificial `stay` between movement commands.

The same nominal test reaches contact at tick 58, about 0.27 seconds sooner, without the reversal. All 12 combinations of four approach geometries at 660/690 speed and actual input arrival −1/0/+1 tick from the 150 ms estimate reach contact without reversing across the target. Tests also introduce a ball after a direct plan is retained and verify that it is rejected. The complete suite passes 107 checks; this verifies the reproduced approach behavior, not all live rescue situations.

## Earlier recorded prediction fixes

The three traces below exposed different prediction failures. The fixes preserve the objective of fast progress toward the exit. They have recorded-frame regressions; a complete live speedrun remains unverified.

| Trace                               | Evidence                                                                                                                                                                                                                                                                      | Change                                                                                                                                                                                                                                                                          |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `death-1789974698172.json`, Area 12 | Changing areas reset the learned delay to 150 ms. Replaying the preceding observations without that reset gives 100 ms before frame 189. The chosen downward dodge's predicted physical clearance falls from 29.76 to 1.46 pixels, below its safety margin.                   | Preserve delay calibration and commands across area changes and pauses; record transition key releases immediately. Reset when the server packet sequence restarts.                                                                                                             |
| `death-1789974880876.json`, Area 13 | After `stay`, observed horizontal speed increases from 270 to 540 rather than decaying to zero. This is consistent with active mouse steering taking over when arrow keys are released. The old trace did not capture the pointer or held keys, so that source is not proven. | Observe analog input and apply the client's no-arrow movement behavior. In the drift hypothesis regression, `stay` changes from +27.51 to −5.13 pixels of physical clearance. New traces capture the missing input state.                                                       |
| `death-1789975183599.json`, Area 30 | Enemy 73 is a large homing ball. After bouncing, it turns back toward the player by approximately 1.5 radians/second. A straight forecast from frame 170 misses its final position by 81.07 pixels.                                                                           | Observe homing parameters and simulate the enemy reacting to each candidate player path. Against the recorded player path, final-position error falls to 4.12 pixels. At frame 187, the retreat's best predicted continuation changes from +29.52 to −6.09 pixels of clearance. |

Homing settings were absent from the older trace. Its regression uses the public client's default 200-unit targeting range and 1.5-radian/second turn rate, consistent with the measured motion. New observations capture per-enemy settings directly. The implementation follows the public client's homing turn and bounce logic, including reverse movement and motion effects. It forecasts pursuit of the local player; target changes between teammates remain a limitation.

Another death arrived during testing: `death-1789975481428.json`, Area 28, from controller revision `8cc9f026a956`. That process had the input diagnostics but had loaded before the homing change. It collided with another homing enemy, ID 677. At frame 190, 350 ms before the recorded impact, the updated forecast lowers the rightward command's clearance from 28.43 to 7.23 pixels. The movement policy immediately overrides its dodge commitment with `focus_down`, predicting 32.52 pixels of clearance. `test/fixtures/recent-homing.json` retains the exact queued-input forecast from that run. This verifies an earlier safety override; it does not prove survival for the rest of that run.

Planning uses separate enemy states for each candidate. Reusing a shared enemy path would incorrectly allow a dodge to assume that a pursuer keeps following a different movement. Angular increments are precomputed and velocity vectors are rotated only when an enemy turns. `artifacts/latest-death-review.json` records the paired planning costs on eight frames from the Area 30 trace, as well as the prediction comparison.

Fixtures are in `test/fixtures/area-delay.json`, `pointer-drift.json`, and `homing-wall.json`. Run `npm test` for the regressions. The checks show the specific forecast errors being corrected, rather than proving that every subsequent action would survive the live run.

Restart the CLI after updates. Each new session logs a controller source fingerprint, actual client held keys, active mouse/gamepad steering, and the queued commands used by the planner. This distinguishes a running older version from a new failure.

## Dense-area planning cost

`death-1789975849059.json` used the completed homing revision and died in Area 35. That area has 100 enemies (70 dashers) and 22 slowing auras. Live planning commonly took 80–128 ms, reducing observation frequency to roughly 7–12 decisions per second. Enemy 1020's forecast matched its recorded final position within 0.02 units; the prominent new issue was computation time.

The planner now indexes enemy swept bounds and aura bounds for each prediction tick. Exact collision and aura calculations operate on local candidates. Bounds include enemy radius, the player's entire next step, and the clearance used by scoring. Clearances beyond the score's saturation threshold are reported as conservative bounds with `clearanceCapped: true`. Homing motion still advances separately for each simulated player path.

The recorded Area 35 regression compares all 17 candidates against the previous exhaustive planner, preserving scores, collision flags, physical clearance for nearby hazards, and planned turns. Additional regressions cover a fast enemy crossing between spatial cells and a large ball whose center is several cells away. Paired timing on Area 35 frames 130–199 reduced median local planning from 32.06 to 11.15 ms and the 95th percentile from 46.16 to 15.41 ms. These measurements exclude browser calls and navigation, so they are not live loop rates. See `artifacts/profile-area35.json`.

The later `death-1789976058400.json` also ran the prior revision. It died in Area 16 at base speed 480 plus a 150 Candy bonus. Its last 60 frames had median planning time 29.69 ms and a maximum of 51.39 ms. The same optimization was profiled on that trace in `artifacts/profile-area16.json`.

Aura presence previously disabled latency fitting throughout an entire area, even when the player was nowhere near one. Calibration now continues for distant auras, while conservatively excluding samples that could intersect an aura. These changes address measured latency and computation problems; survival of either complete run has not been established.

## Tight gaps at Candy speed

`death-1789976314474.json` (Catastrophic Core 13) and `death-1789976453344.json` (Catastrophic Core 11) ran revision `2d426b49df60` at base 510 plus a 150 Candy boost. The first trace has mouse steering off; the second records active analog input, which the planner models. Median live planning was approximately 9–10 ms, confirming that the earlier computation fix was running. Both traces repeatedly selected routes with only a few units of physical clearance after all routes lost their 30.5-unit safety buffer. The flat penalty for breaching that buffer allowed forward progress to outweigh much safer alternatives. Dodge commitment could hold these choices for 100 ms unless the proposed clearance improvement exceeded eight units.

The updated score penalizes the size of the clearance deficit. A five-unit improvement in an already unsafe gap can now interrupt commitment. The first searched action covers about 66 units per axis, reducing its duration from 150 to 100 ms at 660 speed. A previously selected sequence is rechecked against each fresh observation with its turn deadlines anchored to server packets. This avoids discarding an escape just because each replan would otherwise move the first turn later. It never reuses old collision results.

`test/fixtures/turn-deadline.json` preserves Area 11 frames 170, 171, and 175. The retained leftward sequence has over 32 units of physical clearance, with its first turn approaching from 100 to 83.3 ms while the absolute turn packet remains unchanged. At frame 175, where no searched route meets the full buffer, the new choice retains over 24 units rather than the recorded roughly one-unit clearance. Existing homing and pointer regressions retain their original 150 ms first input to isolate the forecast checks; the dense spatial reference was regenerated using exhaustive collision checks with the updated search and score.

The closed-loop comparison in `artifacts/replay-clearance-matrix.json` uses observed enemy positions, interpolates missing server ticks, and simulates player movement and queued inputs. It starts at frames 140 and 160 of both traces and varies actual command delay by minus/plus one tick relative to the prediction. Both controllers survived all 12 short counterfactual windows. Worst physical clearance rose from 4.1 to 24.9 units; forward progress varied, including temporary retreats. This supports increased margin in those encounters, not a claim that the live deaths or a whole level have been solved.

The updated controller cleared six simplified dasher courses at constant 660 speed and 135 ms configured delay (`artifacts/benchmark-clearance-candy.json`). This models the boosted movement speed, not Candy spawning or pickup. Paired local planning on recorded Area 13 frames measured 9.18 ms median / 12.80 ms p95 before and 9.96 ms / 13.29 ms after (`artifacts/profile-clearance.json`), excluding navigation and browser I/O.

The later `death-1789976737617.json` (Central Core Hard 30, 630 speed) also ran the prior revision. At frame 193 its selected route had 12.6 units of predicted clearance; the updated search finds 22.9. By frame 196, already queued movement restricts the clearance to 4.9 regardless of the first input. Earlier escape planning remains essential.

Sweet Tooth automation now reads the actual ability cooldown, level, lock/disable state, energy cost, and confirmed consumption timer. It pulses the default X key in the normal control loop, refreshes the same boost before expiry or for low energy, and starts/changes the boost only while sheltered. Pause releases X with movement. The planner includes a known Candy expiry in its movement forecast. Unit checks cover these conditions; actual automatic pickup remains to be checked in the next live run.

## Dense homing encounter

`death-1789976976296.json`, Catastrophic Core 22, ran revision `b863ea376386` with the turn-continuation and Candy changes. Base speed was 510 plus 150 Candy; mouse steering was off. Of 54 hazards, 34 were homing enemies. Live planning rose to roughly 60–100 ms during the final encounter. The collider, enemy 1362, was forecast within approximately three units at the final observation when supplied the recorded player path from frame 197. By frame 199 the impact was inside the committed-input interval. The larger new problem was the time spent repeatedly advancing every homing enemy for every search branch.

The optimization precomputes each homing enemy's exact motion without a target, including wall bounces and timed effects. A branch activates private pursuit state as soon as its player enters the enemy's range. Spatial queries cover both targeting range and collision radius. Activated pursuers stay private even after leaving range. Vector cross/dot products replace most angle calculations, with the original angle rule retained near numerical boundaries.

Across frames 170–199, all 510 compared candidate scores, paths, and physical clearances match the exhaustive reference. An additional 200 reference forecasts covering range changes, wall bounces, reverse homing, and timed stops had zero position difference. `test/fixtures/dense-homing.json` preserves the recorded dense-case reference. Paired local profiling reduced median planning from 41.48 to 22.97 ms and p95 from 69.06 to 30.95 ms. These measurements exclude browser I/O and navigation and do not establish live survival. The report is `artifacts/homing-death-review.json`; performance details are in `artifacts/profile-homing-dense.json`.

Some other homing enemies turn while outside the local player's targeting range, which is consistent with another player attracting them. The trace did not include other players, so that explanation is unconfirmed. New observations retain other player positions and downed states for diagnosis. Predicting target switches remains a limitation.

## Waiting beside frozen enemies

`death-1789977248805.json`, Catastrophic Core 26, ran `b2ee085a0bd0` with 12 homing enemies among 56 hazards. The player stopped at approximately `(353860, 44)` near the top wall. Across repeated decisions, `stay` scored well because its plan promised forward movement on a later segment. The full-speed `right` candidate already had 41.3 units of predicted physical clearance, but the controller waited for the roughly half-second stationary timeout before taking it. This consumed part of the window while nearby enemies were frozen. Later wall dodges deteriorated into near-zero clearance, and enemy 1908, a dasher, collided with the player.

Waiting now ends after the short commitment when another fully checked candidate advances immediately and scores within 35 points of the wait. This bypasses the longer stall timeout; it does not authorize unsafe movement or force retreat for an ordinary wait. The original stall escape still handles blocked commands and required detours. At recorded frame 153, the new policy chooses `right` with 66 units of first-segment movement and 41.3 units of clearance, while the old policy continued waiting until frame 161. This is an earlier escape opportunity, not proof of survival after the route changes.

Homing enemies whose motion scale is zero throughout the whole forecast now share their frozen collision path. Their collision bodies remain present, and private pursuit simulation resumes whenever any part of the horizon includes motion. All 340 compared candidate results on sampled frames 140–199 match the exhaustive reference. Median local planning fell from 21.35 to 14.28 ms, with p95 falling from 26.02 to 21.19 ms (`artifacts/profile-homing-trap.json`). Regressions are in `test/fixtures/frozen-wait.json` and `frozen-homing.json`.

## Queued turns crossing a server update

`death-1789977468517.json`, Catastrophic Core 22, ran `d9da2a562cd3`, which already included the faster homing simulation. Median planning across its last 40 frames was 34.85 ms. The collider, enemy 1753, was an ordinary ball with nearly constant velocity; no other players were present. At frame 196 the controller selected `up_right`. The latency estimator samples commands at the end of each server update, but the planner sampled them at the start, shifting both queued turns and new actions one update later. A full-speed reversal can differ by 22 units per axis over that update.

The planner now uses the same update-end convention as calibration. At frame 196 the upward reversal changes from 2.1 units of predicted physical clearance to a collision with enemy 1753 (approximately −2.8 units). With the reconstructed navigation map, the selected action becomes `right`. That alternative still has only 2.1 units of clearance to a different ball, so this is evidence of correcting the fatal reversal, not a robust survival guarantee. At frame 197 the already-issued upward reversal makes the same impact unavoidable within the committed prefix.

Synthetic regressions independently verify a turn arriving between updates and a new dodge executing on its first eligible update. `test/fixtures/input-tick-phase.json` retains the recorded reversal. The simplified benchmark and counterfactual replay also now consume commands on update ends. Exhaustive collision references were regenerated with corrected input timing; the historical Area 28 homing comparison explicitly preserves its original executed tick schedule. The continuation test still verifies the absolute turn deadline, now using a downward route with over 27 units of clearance. Its crowded-lane case retains over 21 units rather than choosing the colliding rightward route.

## Apparent drift during a late state read

`death-1789977766043.json`, Central Core Hard 35, ran `74f9b2f1e6d5` with mouse steering off. It had 100 hazards, including 70 dashers, plus 22 slowing auras. Its final two planning calls took 120.04 and 117.69 ms; the last observation round trip was approximately 88 ms. Enemy 1351, a dasher, collided with the player while earlier upward/rightward commands were still taking effect.

Browser capture timestamps and controller request/receipt timestamps reveal an additional timing problem. Reconstructing their clock offset using the shortest round trip puts the final snapshot approximately 79.36 ms before its receipt. The controller previously dated that old position at receipt, making the queued downward turn look approximately 79 ms closer than it was. With the server-update correction alone, the forecast still predicts 20.48 units of physical clearance. Dating the snapshot at capture instead predicts overlap with the actual collider (−26.29 units). At frame 197, before that final committed collision, the same correction lowers `up_right` from 31.32 to 27.03 units, below its 30.5-unit buffer; the local search finds `focus_up` with 31.97 units.

`ObservationClock` now aligns the browser and controller performance clocks from the fastest round trip, clamps estimates to their request intervals, and resets when the browser clock restarts. Latency fitting and pending inputs use the capture time. Snapshot age and measured input overhead are included in the new command's budget. Traces record capture time, snapshot age, observation round-trip time, and input overhead. The retrospective age estimate has uncertainty from the shortest round trip and rounded request timestamps; it is not a server timestamp.

`test/fixtures/snapshot-age.json` checks the earlier route rejection and later committed impact. No additional mouse drift or slippery effect is established by this trace. Both recorded-decision checks hold the observed environment fixed; neither proves complete live survival. Sudden planning or input-cost spikes can still exceed the previous loop's timing estimate.

The comparisons are saved in `artifacts/input-timing-review.json`. All 72 automated checks pass. The final controller cleared all six simplified dasher courses at constant 660 speed and 135 ms configured delay, using the corrected update-end convention (`artifacts/benchmark-input-timing.json`). The simulator does not include browser round-trip stalls or reproduce the complete live Area 35 encounter.

## Stopping at the refuge entrance beside a wall ball

`death-1789978375487.json`, Catastrophic Core 35, ran the completed timing revision `9b0206992d3e`. It contains 150 hazards (76 dashers, 66 slowing enemies, eight perimeter balls) and 66 slowing auras. Mouse steering was off. Late planning generally took 7–20 ms, and snapshot age was tracked at roughly 13 ms. The immediate failure was at the entrance to the active zone, near the top wall.

At frame 187 the controller released `left` for `stay`, forecasting 35.37 units of physical clearance. It expected queued movement to finish bringing the player into the refuge, at which point collision checks stopped considering nearby threats. The observed stop settled at x=381714.28125; full-body shelter required x<=381713. At frame 193 the reported clearance collapsed to 2.17 units. The controller then moved diagonally out as wall ball 1139 turned onto the top edge. Its position on the first tick after frame 199 is consistent with the collision; the ball had moved farther away by the final downed snapshot.

The collision forecast now retains its clearance buffer at refuge entrances until the projected position is well inside shelter. This changes the stopping route at frame 187 to 21.05 units of physical clearance, below its 30.5-unit buffer, while continuing left retains over 34 units. The movement policy continues the retreat. The inset is limited to internal zone boundaries, so a player against an outer map wall deep inside shelter remains protected. Aura speed calculations keep the actual game boundary; only the collision safety assumption becomes more conservative.

The previous perimeter forecast also preserved movement overshoot around a corner. The public client's wall-ball update clamps at the boundary, rotates velocity, and moves along the next edge on a later update. Ball 1139 was at y=32 before its top turn: one 5.5-unit step clamps it at y=30, discarding 3.5 units rather than immediately moving right by that amount. The updated forecast reproduces every recorded position from packet 18841 through 18860 exactly. It also preserves native tick timing when navigation samples every 100 ms, when a timed stop expires, and at a different negotiated tick rate.

`test/fixtures/safe-zone-retreat.json` preserves the stopping decision and wall-ball observations. Regressions cover the recorded retreat, clockwise and counterclockwise turns, timed stops, coarse sampling, and shelter beside an outer wall. The counterfactual replay now applies slowing auras from their recorded positions, with source IDs included in new observations. Older traces require stable aura ordering; missing enemy or aura sources abort the replay.

In `artifacts/replay-refuge-corner.json`, both revisions survive six short windows starting at frames 184 and 187 with actual input timing varied by ±1 tick. Worst physical clearance rises from 1.25 to 33.06 units. For the early-arrival case starting at frame 187, it rises from 1.25 to 37.67. These windows cover only 0.38–0.45 seconds and include temporary retreat; they support improved margin, not a claim that the full live run is solved.

All 76 automated checks pass. The controller also clears all six simplified dasher courses at constant 660 speed and 135 ms configured delay (`artifacts/benchmark-refuge-corner.json`).

The recorded decision and corner comparison are in `artifacts/refuge-corner-review.json`. Paired local profiling on the last encounter remains comparable (7.80 ms median before, 7.29 ms after, excluding navigation and browser calls); see `artifacts/profile-refuge-corner.json`.

## Missing focused follow-up turns in Area 39

`death-1789979352064.json`, Catastrophic Core 39, ran the completed refuge/corner revision `a365fa97cb4d` at base 510 plus 150 Candy. The final snapshot overlaps ordinary ball 828 by 7.51 units. There were 105 hazards, including 15 homing enemies, and 23 slowing auras. Mouse steering was off and no other players were observed. Only two of the final 50 recorded decisions had a route that retained the safety buffer; repeated emergency overrides followed as the available space shrank.

The ordinary search considers all 17 first inputs, but its later segments previously allowed only full-speed directions, waiting, and keeping the same Shift direction. It could not plan a full-speed first dodge followed by a different focused turn. When the ordinary search and retained plan are both unsafe, a recovery pass now considers all directions at each subsequent segment. It reuses the first segments and hazard forecasts, keeps one continuation per first input, and stops after finding a buffered route. Existing safe routes are preserved. A committed input interval or first segment that already breaches the buffer cannot be repaired by later turns and is skipped.

At frame 152, 1.27 seconds before the final downed snapshot, the old local search has no buffered route: its best physical clearance is 26.52 units, below the required 30.5. Without a route map, recovery finds a full-speed downward input followed by focused turns, retaining 32.14 units. With a freshly built navigation map, it finds a full-speed down-left input followed by focused turns, retaining 30.80 units. The latter map is rebuilt from the snapshot, not an exact reconstruction of the live map's earlier history. `test/fixtures/focus-recovery.json` retains the state and queued inputs; tests also check immediate movement-policy selection, preservation of the recovered sequence's deadlines, unchanged ordinary safe routes, and rejection of recovery after a committed collision.

These are recorded-state forecast comparisons, not a replay proving that the altered path survives the rest of the encounter. An extra tick before the new command arrives closes the buffered escape at this frame. Across 60 sampled local planning calls from frames 140–199, median time increases from 10.74 to 12.63 ms and p95 from 18.83 to 20.21 ms. This excludes navigation and browser I/O (`artifacts/profile-focus-recovery.json`). The focused search is conditional to limit that added delay. The paired decisions and timing sensitivity are saved in `artifacts/focus-recovery-review.json`.

All 80 automated checks pass, including four new recovery checks. All six simplified dasher courses clear at constant 660 speed and 135 ms configured delay (`artifacts/benchmark-focus-recovery.json`). This benchmark does not recreate the complete live Area 39 encounter.

## Route arrival times and unnecessary Shift

The later `death-1789980648310.json` ran `dee58f52aa16` in Catastrophic Core 35 with 150 enemies and 66 slowing auras. Its final snapshot overlaps dasher 1492. The last encounter also contains long stretches of little progress, lateral reversals, and focused movement. At frame 111, the controller retained `focus_up_right` even though `up_right` was the highest-scoring route: first-segment progress was 72.32 versus 40.66, and physical clearance was 45.83 versus 38.63. The score difference was only 3.74, below the general turn-switching threshold.

After the short commitment, the movement policy now permits that same-direction acceleration when it is preferred, fully buffered, improves clearance by more than five units, and advances farther immediately. Direction reversals retain their existing hysteresis. The recorded candidates are retained in `test/fixtures/accelerate.json`; a regression also verifies that an unfinished dodge or reduced clearance cannot use this exception.

The previous route map priced every cell at one arrival time estimated from straight-line distance to the current player. Backtracking, waiting, and slowing fields did not update that arrival estimate. It also charged Euclidean distance for diagonals despite the game's full per-axis keyboard speed. The replacement first finds geometric distance to the exit around walls, then works backward through a bounded grid of future times. Moving and waiting consume time; enemy sweeps add a local risk cost. Slowing auras apply once per type, before Candy's bonus, and known Candy expiry changes later travel time. A small lateral tie-break keeps empty corridors straight.

The local dodge search queries this map at each predicted endpoint's actual time. Cached maps retain their source server packet, so those queries include the map's age. The approximately 2.4-second coarse forecast is bounded to 48 time slices, while exact local collision checks retain their existing horizon and safety margins. Beyond the coarse horizon, geometric distance supplies the remaining route. The map's collision costs and aura traversal are approximations, and its homing forecasts still target the current player position; the fine search retains separate pursuit simulation for each path.

Tests cover different arrival times at the same crossing, diagonal timing, straight open lanes, aura stacking, Candy expiry, packet-based map age, and the recorded Shift release. Paired benchmarks now load the baseline's navigation and movement classes as well as its planner. Recorded-window replay uses the recorded reaction budget for both policies and includes navigation work in measured planning time.

While these changes were being tested, `logs/2026-09-21T08-56-41.645Z.jsonl` recorded a run using intermediate revision `60cc8af36234`. It reached Ominous Occult Hard 17, which the downloaded client identifies as a victory checkpoint. The report in `artifacts/latest-run-efficiency.json` measures the observed sections of Areas 1–16. It excludes endpoint waiting and does not count diagonal travel as lost forward speed. Areas 1, 3, and 9 were close to the unobstructed forward rate for their observed stats. The Area 16 sample window spans 20.32 seconds for 5,346 units of forward progress, with approximately 6.1 seconds of focus, 3.1 seconds waiting, and 198 units of sampled backtracking. Its rate is about 40% of the unobstructed stat-based upper bound. That bound ignores enemies and slowing auras; the difference is not all safely recoverable time. Area 2's longer crossing also occurred at base speed 165–195, well before reaching the maximum upgrade.

The ordinary status log lacked the candidate alternatives needed to distinguish an avoidable slow command from a necessary dodge in that successful run. New status rows retain compact candidate scores, clearances, first-segment movement/progress, server packet, and area geometry at the existing one-second cadence. Full death traces remain unchanged.

The final forward shortcut also checks future route cost, rather than relying entirely on a waypoint's lateral offset. A full-speed trajectory can remain clear and reach an equally useful endpoint even when a coarse route bends sideways. It is accepted only when its exact local checks retain the shortcut's clearance threshold and its cost reduction covers the travel interval within less than a grid cell of interpolation tolerance. Regressions check both acceptance of that clear advance and rejection when the timed route budget does not fit.

Validation of the final change:

- All 87 automated checks pass.
- Both versions clear 12/12 simplified dasher courses at constant 660 speed and 135 ms configured input delay. Mean clear time changes only slightly, from 4.378 to 4.358 seconds. Total Shift time falls from 7.78 to 6.34 seconds (18.5%), and command changes from 213 to 186 (12.7%). Individual courses still regress, so these results do not establish a large speedrun improvement. Median local planning is 9.36 versus 9.13 ms; p95 is 11.17 versus 12.53 ms. See `artifacts/benchmark-timed-navigation-dash.json`.
- The deep-pocket course still clears: 3.92 seconds before versus 3.93 after (`artifacts/benchmark-timed-navigation-pocket.json`).
- In the earlier Catastrophic Core 35 recording, both versions survive all nine counterfactual windows starting at frames 0, 80, and 130 with input timing varied by ±1 tick. Worst physical clearance rises from 8.04 to 13.82 units. Forward progress improves in seven windows and worsens in two; total command changes fall from 318 to 285. These cover only 2.2–6.83 seconds each, use recorded enemy positions, and do not reproduce the complete live run. Local planning tails can be higher with timed routing. See `artifacts/replay-timed-navigation.json`.

A faster live completion of the new Ominous Occult Hard run has not been verified. The latest successful run's one-second samples do not contain complete enemy states or alternative plans, so an exact replay of its Area 16 slowdown is unavailable.

## Enemy catalog, Wacky patterns, Slippery and Pumpkins

The September 21 recordings exposed several separate observation and prediction failures:

- Monumental Migration 44 (`death-1790017596998.json`) collided with Switch enemy 1932. The old observer omitted it from all 200 pre-death frames because it was harmless. Tracking now retains harmless bodies and forecasts the expiry of their timers. The regression explicitly reconstructs the missing enemy's short pre-death path from its final velocity and first lethal packet; those missing positions and timer are assumptions, not recorded telemetry.
- Vicious Valley 1 (`death-1790016815325.json`) continued requesting down-right after the native Down key disappeared. The keyboard controller now reconciles the game's actual held keys with its own cache and repairs dropped keys. Sizing phase/radius changes are also recorded and predicted.
- Wacky Wonderland 16 (`death-1790017820447.json`) collided with Spiral 1922. A straight forecast missed its position by about 87 units at a quarter-second. A broad reachable-region fallback prevented reliance on that line but caused excessive retreating. The replacement learns angular acceleration and repeating velocity cycles from past observations. On 131 recorded quarter-second Spiral forecasts with fresh planning observations, mean position error was 0.23 units and maximum error 3.35 units. The new regression also checks a repeating wave on unseen future samples and invalidates it after a pattern change.
- Restless Ridge 1 (`death-1790018474906.json`) kept sliding right while up-right was requested. Slippery aura 53 was absent from observations. Its live radius is now captured; local simulation preserves the entry angle, ignores Shift braking/steering while locked, and handles leaving the aura and outer-wall recovery. The old trace has no aura radius, so its regression checks the recorded locked movement without inventing a level-specific radius.
- The subsequent Restless Ridge 2 recording (`death-1790018889016.json`) contained manual A/W keys alongside bot arrows. WASD now pauses automation, including when those keys were already held before P was pressed.
- Haunted Halls 11 (`death-1790019055647.json`) treated dormant Pumpkin 553 as moving for seconds even though its recorded position stayed fixed. Its cached client prediction velocity was stale. Observation now captures dormancy, windup, activation and charge time; forecast tests cover a stationary dormant body, delayed launch and the charge stopping after its remaining duration. A complete counterfactual replay is unavailable because that trace did not record Pumpkin phase fields and its launch can depend on the player's path.
- Elite Expanse Hard 4 (`death-1790019636290.json`) collided with Star 2046, which alternated between two points 120 units apart every 350 ms. Treating its direction vector as ordinary velocity missed the return landing. Observation now records the public pause timer, interval, distance and direction. At recorded frame 185, reconstructing the missing phase changes straight-right predicted clearance to −21.7 units; the selected up-right alternative has +1.9 units. The fixture labels the timer reconstruction explicitly. Additional tests check forward Teleporting boundary behavior and multiple landings within one coarse step. This does not establish survival of the full level, and internal-obstacle teleport behavior remains incomplete.

The catalog in `data/enemies.json` records 143 enemy types and 88 projectile types from the public client, with a generated [readable directory](enemy-types.md). Catalog completeness does not imply complete movement/attack coverage; [coverage notes](enemy-catalog.md) identify remaining limitations.

`artifacts/replay-wacky-patterns.json` compares the broad fallback and learned forecasts against the same recorded enemy positions. Four starting frames, each tested with input arrival one tick early, on time or late, give 12 windows per model. Both survive all windows. For the longest 3.38-second window with on-time input, the fallback retreats 187.9 units while the learned predictor advances 486.5; focused decisions fall from 61.0% to 8.5%. That window has more action changes with the learned model, so this is not a universal reduction in turning. In the 1.03-second window starting at frame 140, learned motion advances 620 units versus 498.3, takes two turns versus five, and uses no Shift. Minimum physical clearance is 90 units versus 75.5. The automated replay regression preserves that improved forward progress and fewer turns.

These are short counterfactual windows using independently recorded enemy positions, not complete live clears or evidence of globally optimal play. Six simplified constant-660-speed dasher courses also cleared (`artifacts/benchmark-enemy-catalog-dash.json`). Offline Chrome integration checks cover catalog injection, harmless Switch observations, Slippery fields and repairing a dropped key. Restart the CLI to load the final changes; intermediate live revisions do not include later fixes.

The final `npm run simulate:enemies -- --seeds 3` run passed 120 tests, cleared six normal/dasher courses and survived all six short Wacky comparison windows. Its [report](../artifacts/enemy-suite.json) includes a coverage inventory: 124 of the 143 enemy types have no separate family check. The remaining entries have limited checks, sometimes shared by Switch variants; they are not certifications of complete behavior. See [simulation instructions](enemy-simulations.md).
